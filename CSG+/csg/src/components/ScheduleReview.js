import React, {Component} from "react";

export default class ScheduleReview extends Component {

    render() {
        return (
            <div><p>We are in the schedule component</p></div>
        )
    }
}